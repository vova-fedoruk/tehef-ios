// Copy to Config/Secrets.xcconfig and set your API host.
API_BASE_URL = https:/$()/tehef.io

// Cloudflare Turnstile site key (same as NEXT_PUBLIC_TURNSTILE_SITE_KEY on the server).
// Leave empty to hide the widget in dev. Test key (always passes): 1x00000000000000000000AA
TURNSTILE_SITE_KEY =
